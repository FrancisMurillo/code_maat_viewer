import { defineMessages } from "react-intl";

export default defineMessages({
    "record": {
        "id": "tab.record",
        "description": "Tab label for record",
        "defaultMessage": "Records"
    }
});
